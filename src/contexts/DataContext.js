// Boilerplate necessary for context-provider:

import { createContext } from "react"
// choose variable name; set it to createContext(null)
const DataContext = createContext(null)

export default DataContext